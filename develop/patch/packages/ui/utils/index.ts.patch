diff --git a/packages/ui/utils/index.ts b/packages/ui/utils/index.ts
--- a/packages/ui/utils/index.ts
+++ b/packages/ui/utils/index.ts
@@ -122,6 +122,9 @@ export function formatIPv6(ip: string) {
 export function transformEndpointURL(url: string) {
+  // fnOS: 以 / 开头的相对路径（如 /app/metacubexd/clash-api）直接返回，
+  // 让浏览器用当前 origin + 相对路径访问，不补 protocol
+  if (url.startsWith('/')) return url
   return url.startsWith('http://') || url.startsWith('https://')
     ? url
     : `${typeof window !== 'undefined' ? window.location.protocol : 'http:'}//${url}`
